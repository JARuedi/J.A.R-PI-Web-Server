services:
  app:
    build: .
    container_name: digital_planner
    restart: always
    ports:
      - "80:3000"
    env_file:
      - .env
    volumes:
      - ./dp_spread_sheet.ods:/app/dp_spread_sheet.ods
      - /mnt/ssd1/mysql_data:/var/lib/mysql
